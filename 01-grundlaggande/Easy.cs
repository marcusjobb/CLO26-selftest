namespace CSharpRepetition;

public class Easy
{
    // Räkna ut totalt antal sittplatser.
    // Enkla stolar = 1 plats, dubbla stolar = 2 platser.
    // RäknaSittplatser(3, 2) => 7
    public int RäknaSittplatser(int enkla, int dubbla)
    {
        int summa = 0;

        // Skriv din kod här


        return summa;
    }

    // Returnera sant om talet är jämnt, annars falskt.
    // ÄrJämnt(4) => true   ÄrJämnt(7) => false
    public bool ÄrJämnt(int tal)
    {
        bool svar = false;

        // Skriv din kod här


        return svar;
    }

    // Summera alla heltal från 1 upp till och med n.
    // SummeraTill(4) => 1+2+3+4 = 10
    public int SummeraTill(int n)
    {
        int summa = 0;

        // Skriv din kod här


        return summa;
    }

    // Returnera det största av tre tal.
    // MaxTal(1, 5, 3) => 5
    public int MaxTal(int a, int b, int c)
    {
        int störst = a;

        // Skriv din kod här


        return störst;
    }

    // Returnera det minsta av två tal.
    // MinTal(7, 3) => 3
    public int MinTal(int a, int b)
    {
        int minst = a;

        // Skriv din kod här


        return minst;
    }

    // Upprepa texten det angivna antalet gånger.
    // Upprepa("ha", 3) => "hahaha"
    public string Upprepa(string text, int gånger)
    {
        string resultat = "";

        // Skriv din kod här


        return resultat;
    }

    // Returnera sant om 'tal' är jämnt delbart med 'delare'.
    // ÄrDelbart(10, 5) => true    ÄrDelbart(10, 3) => false
    public bool ÄrDelbart(int tal, int delare)
    {
        bool svar = false;

        // Skriv din kod här


        return svar;
    }
}
