namespace CSharpRepetition;

public class Easy
{
    // Räkna hur många vokaler (a, e, i, o, u, y, å, ä, ö) texten innehåller.
    // Ignorera skillnad mellan stora och små bokstäver.
    public int RäknaVokaler(string text)
    {
        int antal = 0;

        // Skriv din kod här


        return antal;
    }

    // Vänd på strängen och returnera den baklänges.
    // VändSträng("hund") => "dnuh"
    public string VändSträng(string text)
    {
        string resultat = "";

        // Skriv din kod här


        return resultat;
    }

    // Kontrollera om ordet är ett palindrom (läses likadant framifrån och bakifrån).
    // Ignorera skillnad mellan stora och små bokstäver.
    // ÄrPalindrom("Anna") => true
    public bool ÄrPalindrom(string ord)
    {
        bool svar = false;

        // Skriv din kod här


        return svar;
    }

    // Växla skiftläge för varje bokstav: liten → stor, stor → liten.
    // Tecken som inte är bokstäver lämnas orörda.
    // VäxlaSkiftläge("Hej!") => "hEJ!"
    public string VäxlaSkiftläge(string text)
    {
        string resultat = "";

        // Skriv din kod här


        return resultat;
    }

    // Räkna hur många ord en mening innehåller.
    // Ord separeras av mellanslag. Flera mellanslag i rad räknas inte som extra ord.
    // RäknaOrd("hej du där") => 3
    public int RäknaOrd(string mening)
    {
        int antal = 0;

        // Skriv din kod här


        return antal;
    }
}
