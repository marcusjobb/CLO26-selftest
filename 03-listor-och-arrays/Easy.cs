namespace CSharpRepetition;

public class Easy
{
    // Summera alla tal i arrayen.
    public int Summera(int[] tal)
    {
        int summa = 0;

        // Skriv din kod här


        return summa;
    }

    // Returnera det största värdet i arrayen.
    // Använd inte Array.Max eller Linq. Loopa igenom arrayen.
    public int HittaMax(int[] tal)
    {
        int max = tal[0];

        // Skriv din kod här


        return max;
    }

    // Vänd på arrayen och returnera den baklänges.
    // Använd inte Array.Reverse. Skapa en ny array.
    // VändArray([1, 2, 3]) => [3, 2, 1]
    public int[] VändArray(int[] tal)
    {
        int[] resultat = new int[tal.Length];

        // Skriv din kod här


        return resultat;
    }

    // Räkna hur många gånger 'sökvärde' förekommer i arrayen.
    public int RäknaFörekomster(int[] tal, int sökvärde)
    {
        int antal = 0;

        // Skriv din kod här


        return antal;
    }

    // Returnera summan av alla JÄMNA tal i arrayen.
    public int SummeraJämna(int[] tal)
    {
        int summa = 0;

        // Skriv din kod här


        return summa;
    }
}
