namespace CSharpRepetition;

public class Medium
{
    // Returnerar poängen för det bästa paret (parets värde × 2).
    // Om inget par finns returneras 0.
    // Par([5, 5, 3, 2, 1]) => 10
    // Par([6, 6, 5, 5, 1]) => 12   (sexorna är bästa paret)
    // Par([1, 2, 3, 4, 5]) => 0
    public int Par(int[] tärningar)
    {
        // Skriv din kod här
        // Tips: hitta alla par och returnera det med högst värde × 2

        return 0;
    }

    // Returnerar summan av de två bästa paren.
    // Om det inte finns två par returneras 0.
    // TvåPar([6, 6, 4, 4, 1]) => 20
    // TvåPar([5, 5, 3, 2, 1]) => 0   (bara ett par)
    public int TvåPar(int[] tärningar)
    {
        // Skriv din kod här

        return 0;
    }

    // Returnerar trekindets värde × 3.
    // Om ingen trio finns returneras 0.
    // Trekind([3, 3, 3, 1, 2]) => 9
    // Trekind([2, 2, 3, 4, 5]) => 0
    public int Trekind(int[] tärningar)
    {
        // Skriv din kod här

        return 0;
    }

    // Returnerar fyrkindets värde × 4.
    // Om inget fyrkind finns returneras 0.
    // Fyrkind([6, 6, 6, 6, 1]) => 24
    // Fyrkind([6, 6, 6, 1, 1]) => 0
    public int Fyrkind(int[] tärningar)
    {
        // Skriv din kod här

        return 0;
    }

    // Returnerar summan av alla fem tärningar om handen är en kåk
    // (exakt en trio och ett par). Annars 0.
    // Kåk([3, 3, 3, 5, 5]) => 19
    // Kåk([3, 3, 3, 3, 5]) => 0   (fyrkind, inte kåk)
    // Kåk([1, 2, 3, 4, 5]) => 0
    public int Kåk(int[] tärningar)
    {
        // Skriv din kod här

        return 0;
    }

    // Returnerar 50 om alla fem tärningar är likadana. Annars 0.
    // Yatzy([4, 4, 4, 4, 4]) => 50
    // Yatzy([4, 4, 4, 4, 5]) => 0
    public int Yatzy(int[] tärningar)
    {
        // Skriv din kod här

        return 0;
    }
}
